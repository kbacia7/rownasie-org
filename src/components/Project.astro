---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="flex flex-row items-center justify-around">
  <div>
    <h3 class="text-4xl font-bold mb-3">{name}</h3>
    <p class="text-xl">
      <slot />
    </p>
  </div>
  <button class="btn btn-secondary">Więcej</button>
</div>
