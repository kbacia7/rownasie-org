---
import type { HTMLTag, Polymorphic } from "astro/types";

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }>;

const { class: classes, as: Tag = "button", ...props } = Astro.props;
---

{/* NOTE: Website uses only one button type, so primary is hard coded */}
<Tag
  class:list={[
    "bg-primary border-primary-600 b-1 hover:bg-primary-400 rounded-md px-4 py-3 text-lg text-white lg:px-5 lg:py-4",
    classes,
  ]}
  {...props}
>
  <slot />
</Tag>
