---
import getProjectsStaticPaths from "@helpers/collections/getProjectsStaticPaths";

export async function getStaticPaths() {
  return await getProjectsStaticPaths("en");
}

const { Content } = Astro.props;
---

<Content />
